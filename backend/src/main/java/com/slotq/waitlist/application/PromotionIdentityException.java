package com.slotq.waitlist.application;

/** The same logical promotion identity was presented with another immutable meaning. */
public final class PromotionIdentityException extends RuntimeException {
    public PromotionIdentityException() { super("Promotion identity meaning does not match"); }
}
